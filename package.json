{
  "name": "dgi-quantrum",
  "version": "1.0.0",
  "private": true,
  "description": "DGI Quantrum - Infraestructura de crecimiento B2B basada en IA para organizaciones con redes de distribuidores",
  "scripts": {
    "start": "cd apps/api && npx tsx src/index.ts",
    "setup:db": "cd apps/api && npm run prisma:generate && npm run db:push && npm run db:seed",
    "dev:api": "cd apps/api && npm run dev",
    "dev:web": "cd apps/web && npm run dev",
    "dev": "concurrently -n api,web -c blue,magenta \"npm:dev:api\" \"npm:dev:web\"",
    "build": "cd apps/api && npm run build && cd ../web && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}